package object;

import java.util.Scanner;

public class MainQ6 {
	public static void main(String[] args) {
	/*
		Scanner scan = new Scanner(System.in);

        // 입력 받기
        int num = input(scan);

        // 연산하기
        boolean isPrime = process(num);

        // 출력하기
        output(num, isPrime);
        */
    }
}